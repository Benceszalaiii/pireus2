function toggleDarkmode(){  // Handle toggle darkmode button
    if (document.body.classList.contains("dark"))
        document.body.classList.remove("dark")
    else
        document.body.classList.add("dark")
}


